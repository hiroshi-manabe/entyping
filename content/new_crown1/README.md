# New Crown 1 Package

This package contains the textbook-aligned content used by the typing app.

Structure:

- `source/`
  - original TSV and combined Japanese notes
- `content.json`
  - generated app-ready JSON
- `audio/`
  - local MP3 assets

The typing program should consume the generated dataset and audio paths, not the raw source files directly.
