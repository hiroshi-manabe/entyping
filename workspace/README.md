# Workspace

This directory is for investigation and intermediate files that are neither:

- application code under `app/`
- nor runtime content packages under `content/`

Use it for:

- downloaded upstream files
- reverse-engineering notes
- extraction experiments
- temporary comparison outputs
- one-off parsing work that may later become reusable scripts

Current workspace area:

- `new_crown1/`
  - textbook-specific investigation material
