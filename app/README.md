# App Area

This directory is reserved for the typing program itself.

Expected responsibilities:

- selection UI
- typing loop
- input handling
- timing and scoring
- audio playback
- loading a content package through a generic schema

The app should not contain textbook-specific parsing or content-build logic.
